'use client';

// Import various Chakra UI components and hooks to see what's available
import {
  Box,
  Button,
  Container,
  Flex,
  Heading,
  Link,
  Text,
  Image,
  Stack,
} from '@chakra-ui/react';

// Try to import ColorMode hooks
import {
  useColorMode,
  ColorModeProvider,
} from '@chakra-ui/color-mode';

// Try to import useColorModeValue
import { useColorModeValue } from '@chakra-ui/color-mode';

export {
  // Components
  Box,
  Button,
  Container,
  Flex,
  Heading,
  Link,
  Text,
  Image,
  Stack,
  
  // ColorMode
  useColorMode,
  ColorModeProvider,
  useColorModeValue,
}; 